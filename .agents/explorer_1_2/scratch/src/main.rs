fn main() {
    let mut renderer: Option<vello::Renderer> = None;
    // Let's see if we can check the methods on Renderer
    // This will trigger compiler errors that show the signature and methods
}
